# CAV 接入执行清单（2026-08-06 更新）

> 组长视角的 C 侧/联动任务总表。✅=已完成，⬜=待办，⚠️=依赖他人确认。
> 使用说明：`项目目录/包或模块的说明文件/CavIdmMovement 结构及使用说明.md`（v2.0，三份旧文档已归档至 `_archived/`）

---

## 一、✅ 已完成

| # | 任务 | 产物 | 验证 |
|---|---|---|---|
| C-1 | Step 0：行人速度兜底修复 | `项目目录/simulation/movement_cav.py`（+8 行） | 单测 1.30 m/s；端到端无卡死 ✅ |
| C-2 | Step 2：union_pack 打包采集 | `项目目录/simulation/cav_pack.py` | 自测 + 真实引擎采样 ✅ |
| C-3 | Step 4：IDM/CAV 离线对比 | `项目目录/compare_cav.py` | 聚合逻辑断言全过 ✅ |
| C-4 | CavSimulator 旧实现归档 | `工作区/C Simmey/algorithms/_archived/` | 项目目录无 import 依赖 ✅ |
| C-5 | 文档合并 | `包或模块的说明文件/CavIdmMovement 结构及使用说明.md`（v2.0） | 三份旧文档已归档 ✅ |
| C-6 | 参数统一入 config | `项目目录/config.yaml` 新增 `cav:` + `micro_fleet:` 段 | get_config 解析验证 ✅ |
| **F-1** | **trip 行程钩子（引擎侧）** | `simulation/engine.py`（L52/191-194/256-258/390-397/542-546） | **✅ F 已合入**；compare_cav 端到端跑通 |
| **F-2** | **union_pack 接线（2 key）** | `simulation/main.py`（L287/315-316） | **✅ F 已合入**；union_pack 带 cav_stats / micro_validation_results |
| **C-7** | **编队实时帧模块** | `项目目录/simulation/micro_fleet.py`（挑车多大门→固定O-D、坐标插值、加速度差分、帧组装） | **✅ 单测通过 + 真实引擎 12/12 采样点抓到 1~4 辆编队车** |
| C-8 | **正式对比（FR-15 量化）** | `项目目录/data/micro_validation_results.json` | **✅ 1000人/1000车/1h/7200tick：通行时间 -3.2%、速度 7.04→7.46 km/h、canteen_1 +20.1%**；密度越高 CAV 优势越大（0.5h 密集窗口 -9.1%） |

## 二、⬜ 待办（按依赖顺序）

### 阶段 A：F-3 微包发射通道（唯一剩余 F 侧改动，规格已在合并文档 6.3 附代码）
| # | 任务 | 文件 | 验收 |
|---|---|---|---|
| F-3a | 驱动循环加 `on_tick(engine, tick)` 回调（~2 行） | `simulation/sender.py::run_and_send` + `csv_recorder.py::run_and_record_send` | 每 tick 回调触发 |
| F-3b | `micro_fleet.enabled=true` 时每 interval tick 组装帧并 POST | `simulation/main.py`（复用 `micro_fleet.collect_micro_fleet`） | 演示窗口 1s 一帧 |
| F-3c | `BackendClient.post_json(endpoint, payload)` 通用方法 | `simulation/backend_client.py` | 微包 POST 成功 |

### 阶段 C：B 后端
| # | 任务 | 端点 |
|---|---|---|
| B-1 | union_pack 入库 | `POST /api/v1/sim/batch`（config 已指向） |
| B-2 | 编队最新帧接收（只存最新，不落库） | `POST /api/v1/sim/micro-fleet` |
| B-3 | 编队帧查询（snake→camel 转换） | `GET /api/v1/vehicle/cav-formation` |
| B-4 | 预警/热力/预测/门闸/随机触发端点补齐 | 见前端 api/index.ts 缺口清单 |

### 阶段 D：E 前端（8/7）
| # | 任务 |
|---|---|
| E-1 | 每秒轮询 cav-formation 渲染 4 车编队 + routeNodes 高亮 |
| E-2 | union_pack 新增段（cav_stats / micro_validation_results）上屏 |
| E-3 | 预警响应结构适配（后端 `data.items`） |
| E-4 | 去 mock（topology.json 保留作图底，数据接真实接口） |

## 三、⚠️ 演示日提醒

- `config.yaml micro_fleet.enabled` 默认 **false**；8/7 演示窗口改为 **true** 再跑 `main.py run`
- `micro_fleet.dst_node_id` 默认 `canteen_1`，但正式对比显示其到达量偏少（吞吐=2）；编队频繁不足 4 辆时
  切换为高流量节点：`east_dorm_8_11` / `west_dorm_5_8`（吞吐 40+）
- 前端示例速度 15.5 m/s 仅为示意：统一园区口径 **5.0 m/s**，动画提速用前端渲染倍率

## 四、关键日期

- **8/6 晚**：F-3 转 F 确认 ✅（本清单 + 合并文档 6.3）
- **8/7 上午**：F-3 合入 + B 端点 + E 渲染联调
- **8/7 下午**：8000 人压测（NFR-01）+ 全链路演示（micro_fleet.enabled=true 演示窗口）
- **8/8 12:00**：提交（README/运行文件/PPT 整理见组长总清单）

## 五、风险与降级

| 风险 | 降级 |
|---|---|
| F-3 未在 8/7 上午前合入 | 编队帧先落盘 `data/micro_fleet.json`（E 直接读文件演示），后端通道后补 |
| 某时刻目标 O-D 候选不足 4 辆 | 帧输出实际在途数（1~4 辆），前端按数组长度渲染；或换高流量 dst_node_id |
| 1s 频率后端压力 | 演示窗口才开（micro_fleet.enabled），B 只存最新帧不落库 |
