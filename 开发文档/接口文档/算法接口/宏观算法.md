# 宏观算法接口

> 对应需求：FR-06（拓扑网络构建）、FR-07（Dijkstra 最短路径）、FR-08（PageRank/中介中心性）、FR-09（AttractRank 热点分析）

## TrafficNetworkAnalyzer

- **所在模块**：待补，如 `algorithms/macro_topo.py`
- **对外接口**：sklearn 风格
- **对应需求**：FR-06/FR-08

### 构造参数（不加下划线）

待补

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(graph) | self |
| transform | transform(node) | 待补：热度得分 |

### 学习属性（尾下划线 _）

| 属性 | 说明 |
|---|---|
| pagerank_ | 各节点 PageRank 得分 |
| betweenness_ | 各节点中介中心性 |

### 使用示例

待补

## ShortestPathFinder

- **所在模块**：待补，如 `algorithms/macro_path.py`
- **对外接口**：sklearn 风格
- **对应需求**：FR-07

### 构造参数（不加下划线）

待补

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(graph) | self |
| predict | predict(src, dst) | 待补：最短路径 |

### 使用示例

待补

## AttractRankAnalyzer

- **所在模块**：待补，如 `algorithms/macro_attractrank.py`
- **对外接口**：sklearn 风格
- **对应需求**：FR-09

### 构造参数（不加下划线）

待补

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(graph) | self |
| transform | transform() | 待补：热点区域 |

### 使用示例

待补
