# 微观与车辆算法接口

> 对应需求：FR-12（CAV 车联网一致性模拟）、FR-13（车辆准入）、FR-14（差分隐私 DP）、FR-15（CAV 提速量化）

## CavSimulator

- **所在模块**：待补，如 `algorithms/cav_sim.py`
- **对外接口**：sklearn 风格
- **对应需求**：FR-12/FR-15

### 构造参数（不加下划线）

待补

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(flow_config) | self |
| predict | predict(horizon) | 待补：通行效率指标对比 |

### 使用示例

待补

## VehicleAccessPolicy

- **所在模块**：待补，如 `algorithms/vehicle_access.py`
- **对外接口**：sklearn 风格
- **对应需求**：FR-13

### 构造参数（不加下划线）

待补

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(density_stats) | self |
| predict | predict(vehicle_request) | 待补：准入/拒入/限制路线 |
| predict_proba | predict_proba(vehicle_request) | 待补：准入概率（如有） |

### 使用示例

待补

## DpNoiseScaler

- **所在模块**：待补，如 `algorithms/dp_noise.py`
- **对外接口**：sklearn 风格（Transformer 式）
- **对应需求**：FR-14

### 构造参数（不加下划线）

待补

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(paths) | self |
| transform | transform(paths) | 待补：加噪后的车辆路径 |

### 使用示例

待补
