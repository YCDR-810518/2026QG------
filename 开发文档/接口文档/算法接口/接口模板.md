# 算法接口模板

> 填写说明：新建算法接口文档时复制本文件，按节填写。完整示例见文末《DensityPredictor》。

## 类名

- **所在模块**：models/xxx.py
- **对外接口**：sklearn 风格（详见《变量及接口命名规范.md》）
- **对应需求**：FR-xx

### 构造参数（不加下划线）

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| 待补 | 待补 | 待补 | 待补 |

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(X, y=None) | self |
| 待补 | 待补 | 待补 |

### 学习属性（尾下划线 _）

| 属性 | 说明 |
|---|---|
| n_features_in_ | 输入特征数（如适用） |

### 使用示例

```python
待补
```

---

## 填写说明

1. 构造参数 = `__init__` 参数，不加下划线；数据学到的属性加下划线
2. 方法签名统一遵守《变量及接口命名规范.md》第 2 节
3. 每个类一段，按《接口规范.md》中的类清单补齐

## 完整示例：DensityPredictor

- **所在模块**：models/density.py
- **对外接口**：sklearn 风格（内部实现使用 MindSpore，详见《变量及接口命名规范.md》附录 C）
- **对应需求**：FR-21

### 构造参数（不加下划线）

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| n_layers | int | 3 | 网络层数 |
| lr | float | 1e-3 | 学习率 |

### 方法说明

| 方法 | 签名 | 返回 |
|---|---|---|
| fit | fit(X, y=None) | self |
| predict | predict(X) | ndarray，预测密度 |

### 学习属性（尾下划线 _）

| 属性 | 说明 |
|---|---|
| n_features_in_ | 输入特征数 |

### 使用示例

```python
from models.density import DensityPredictor

predictor = DensityPredictor(n_layers=3, lr=1e-3).fit(X_train, y_train)
pred = predictor.predict(X_test)
```
